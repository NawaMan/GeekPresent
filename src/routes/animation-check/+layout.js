// Deck root — prerender the whole deck; deck URLs carry a trailing slash
// (each slide dir overrides with trailingSlash "never").
export const prerender = true;
export const trailingSlash = "always";
