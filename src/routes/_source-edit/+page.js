// Unscaled source editor popup — outside every deck layout so there is no
// canvas `transform: scale(…)`. Prerendered shell; content arrives via
// sessionStorage / postMessage from the opening slide.
export const prerender = true;
export const trailingSlash = 'never';
