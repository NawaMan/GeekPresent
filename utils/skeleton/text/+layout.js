// A Text is a single route, not a deck: prerendered, and addressed without a
// trailing slash (it is /<name>.html, the same shape as a slide URL).
export const prerender = true;
export const trailingSlash = "never";
