<!--
  Your first Text. This is plain authored markup — write prose, drop in $lib components,
  colocate an image next to this file and import it. There is no slide list and no
  pagination: the page simply grows downward.
-->
<h1>My Text</h1>

<p>
	Replace this with your own writing. A Text is one hand-authored page — the
	read-at-your-own-pace counterpart to a slide deck, built from the same components.
</p>

<h2>Write it like a document</h2>

<p>
	Headings, paragraphs and lists flow down a single column. You can reach for any component
	the decks use, import an image sitting next to this file, and style locally when you need
	to. See <code>.samples-ref/text.html/</code> for a worked example.
</p>
