// Deck-level: prerender to static HTML, and address slides WITH a trailing slash.
// (Each slide dir under here sets trailingSlash "never" — see title.html/+layout.js.)
export const prerender = true;
export const trailingSlash = "always";
